# DocPrompt-Hook

A pre-commit hook that prompts a user to update documentation. The hook will ask if the changes
effect current documentation. If the user answers yes it will check the commit to ensure that a
documentation file is included in the commit.
